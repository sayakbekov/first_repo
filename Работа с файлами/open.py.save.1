'''
file = open('file.txt', 'w')
name = input('your name? ')
file.write(name)
file.close()

with open('file.txt', 'r') as f:

        content = f.read()
        print(content)


'''



'''
with open('/home/beknazar/Pictures/Wallpapers/yin.jpg', 'rb') as file:
        byte = file.read()
        print(byte)
'''
'''
with open ('parizh-frantsiia-graffiti-strit-art.jpg', 'rb' ) as file:
        byte = file.read()

with open ('yin.jpg', 'wb') as f:
        f.write(byte)
''' 

'''
file = open('users.txt', 'w')
login = input('login')
password = input('password')
file.write(login)
file.write(password)
file.close()
'''
'''
file = open('directories.txt', 'w')
file.write(' 8dan.py                 python_lessons   snap    Документы   Изображения   Общедоступные   Шаблоны
 Firefox_wallpaper.png  smart&rich      Видео   Загрузки    Музыка       Рабочий стол')
file.close()


f =  open('directories.txt', 'r')
a = f.read()
print(a)
f.close()
'''
'''
file = open('text.txt', 'w')
file.write('Python is a widely used high-level programming language for general-purpose
programming, created by Guido van Rossum and first released in 1991. An interpreted
language, Python has a design philosophy that emphasizes code readability (notably
using whitespace indentation to delimit code blocks rather than curly brackets or
keywords), and a syntax that allows programmers to express concepts in fewer lines of
code than might be used in languages such as C++ or Java.')
file.close()

f =  open('text.txt', 'r')
a = f.read()
if 'w' in a:
	print('Да, в тексте есть w')
else:
	print('Нет, в тексте нет w')
f.close()
'''
''''
t_words = []
file = open('python.txt', 'w')
file.write(''Python is a widely used high-level programming language for general-purpose
programming, created by Guido van Rossum and first released in 1991. An interpreted
language, Python has a design philosophy that emphasizes code readability (notably
using whitespace indentation to delimit code blocks rather than curly brackets or
keywords), and a syntax that allows programmers to express concepts in fewer lines of
code than might be used in languages such as C++ or Java.')
file.close()

f = open('python.txt', 'r')
a = f.read()
for i in a.split( ):
        if 'T' and 't' in i:
                t_words.append(i)
print(t_words)
f.close()
'''

import userler

file = open('userler.py', 'r')
choice = input('войти или зарегистрироватся? ')
print(userler.users.keys())
print(userler.users.values())
if choice == 'войти':
	l = input('username: ')
	for i in userler.users:
		for k in userler.users.keys():
			if k  == l:
				password = int(input('password: '))
				for p in userler.users.values():
					if password == p:
						print('welcome!')
					else:
						print('такого пароля не существует!')
			else:
				print('такого аккаунта не существует!')


if choice == ('зарегистрироватся '):
	file = open('userler.py', 'r')
	l = input('login ')
	for in 
